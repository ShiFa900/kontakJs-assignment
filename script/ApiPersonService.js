/**
 * Provides CRUD functionality of Person data from/into the Backend API
 */
export class ApiPersonService {
  /**
   * Returns array of all Persons data from the backend
   */
  getAll() {}

  /**
   * Creates a new Person data into the backend
   * @param {*} person Person
   */
  create(person) {}

  /**
   * Updates (PUT) existing Person data with the given UUID
   * @param {*} uuid string
   * @param {*} person Person
   */
  update(uuid, person) {}

  /**
   * Deletes existing Person data with the given UUID
   * @param {*} uuid
   */
  delete(uuid) {}
}
